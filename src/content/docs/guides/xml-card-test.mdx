---
title: XmlCard Test Page
description: Visual test page for the XmlCard component display modes
---

import XmlCard from '@/components/xml-viewer/XmlCard.astro';
import characters from '@/generated/characters.json';

export const byFile = (filename) => characters.find((c) => c.filename === filename);
export const tanadirian = byFile('tanadirian');
export const murky = byFile('murky');
export const malfunction = byFile('malfunction');
export const delizan = byFile('delizan');
export const dorian = byFile('dorian');
export const dougal = byFile('dougal');
export const darlo = byFile('darlo');
export const malagolvir = byFile('malagolvir');
export const garaele = byFile('garaele');
export const milo = byFile('milo');
export const alioth = byFile('alioth');
export const antonidas = byFile('antonidas');
export const akinori = byFile('akinori');

This page demonstrates the `XmlCard` component in its display modes and
configuration options. Unlike golden-forest's `XmlChar` (which takes a
`filename` and parses XML in the browser), `XmlCard` takes build-time parsed
`character` JSON — missing files therefore fail the build instead of
rendering an error card.

Character data comes from the `src/generated/characters.json` build artifact
(a single array; look entries up by `filename`):

```mdx
import XmlCard from '@/components/xml-viewer/XmlCard.astro';
import characters from '@/generated/characters.json';

export const tanadirian = characters.find((c) => c.filename === 'tanadirian');

<XmlCard character={tanadirian} display="small" />
```

## Display: Small

Compact card: name, race/class/level, alignment/background/deity, vitals grid,
compact abilities (MOD + SAVE). Avatar is the same size as medium/large (64px).
Small cards are `inline-block`, so they flow side by side.

### Single Character

```mdx
<XmlCard character={tanadirian} display="small" />
```

<XmlCard character={tanadirian} display="small" />

### Multiple Characters (Default Flow)

Small cards flow naturally side by side without any wrapper.

```mdx
<XmlCard character={murky} display="small" />
<XmlCard character={malfunction} display="small" />
```

<XmlCard character={murky} display="small" />
<XmlCard character={malfunction} display="small" />

### Multiple Characters (Centered)

Wrap them in a centered container.

```mdx
<div style={{ textAlign: 'center' }} class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 not-content">
  <XmlCard character={delizan} display="small" />
  <XmlCard character={dorian} display="small" />
  <XmlCard character={dougal} display="small" />
</div>
```

<div style={{ textAlign: 'center' }} class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 not-content">
  <XmlCard character={delizan} display="small" />
  <XmlCard character={dorian} display="small" />
  <XmlCard character={dougal} display="small" />
</div>

## Display: Medium

Everything from small mode, plus full ability grid, proficient-only saving
throws, skills, languages, and proficiency bonus. This is the default size.

### Basic Usage

```mdx
<XmlCard character={tanadirian} display="medium" />
```

<XmlCard character={tanadirian} display="medium" />

### Different Character

```mdx
<XmlCard character={malagolvir} display="medium" />
```

<XmlCard character={malagolvir} display="medium" />

### Two Cards (PC Check)

Verify two medium cards display side by side on PC workstation screens.

```mdx
<XmlCard character={tanadirian} display="medium" />
<XmlCard character={malagolvir} display="medium" />
```

<div class="grid grid-cols-1 sm:grid-cols-2 gap-4 not-content">
<XmlCard character={tanadirian} display="medium" />
<XmlCard character={malagolvir} display="medium" />
</div>

## Display: Large

The complete card: portrait (floated right), feats, features grouped by
level, and powers grouped by level and group. Includes vertically stacked
S/M/L toggle buttons on the right side of the header to switch display
modes live.

### Subclass

Darlo has a subclass (Oath of Vengeance), shown in parentheses after
class and level. Large mode is the only mode that displays it; toggle to
S or M to see it hidden.

```mdx
<XmlCard character={darlo} display="large" />
```

<XmlCard character={darlo} display="large" />

Antonidas exercises the feature-entry fallback: his class node has no
`<specialization>`, so the parser derives `School of Transmutation` from
the subclass features in the sheet.

```mdx
<XmlCard character={antonidas} display="large" />
```

<XmlCard character={antonidas} display="large" />

### Fallback Portrait

Garaele has no avatar file, so the build-time probe falls back to
`faceless.svg`. Forcing a non-existing `image` resolves to the same
fallback at build time.

```mdx
<XmlCard character={garaele} image="dontexists.jpg" display="large" />
```

<XmlCard character={garaele} image="dontexists.jpg" display="large" />

```mdx
<XmlCard character={garaele} display="large" />
```

<XmlCard character={garaele} display="large" />

### Default Portrait (JPG, by Filename)

Milo relies on the default naming convention: no `image` prop, so the
portrait resolves from the XML filename (`milo.xml` → `milo.jpg`).

```mdx
<XmlCard character={milo} display="large" />
```

<XmlCard character={milo} display="large" />

### Explicit Portrait (JPG) + Long Deity

Alioth exercises an exceptionally long deity field, and the S/M/L toggle
above the portrait switches the card between display modes.

```mdx
<XmlCard character={alioth} display="large" image="alioth.jpg" />
```

<XmlCard character={alioth} display="large" image="alioth.jpg" />

### Explicit Portrait (PNG)

```mdx
<XmlCard character={antonidas} display="large" image="antonidas.png" />
```

<XmlCard character={antonidas} display="large" image="antonidas.png" />

### Feature Grouping

Akinori has 19 features across several levels, showing the per-level
grouping with expand/collapse details.

```mdx
<XmlCard character={akinori} display="large" />
```

<XmlCard character={akinori} display="large" />

### Powers Grouping

Dougal has 2 powers, showing the grouping by level and group.

```mdx
<XmlCard character={dougal} display="large" />
```

<XmlCard character={dougal} display="large" />

### Passive Skills

Large mode adds a Passive Skills section between Abilities and Saving Throws.
The section renders no visible heading: hovering it reveals a "Passive Skills"
tooltip. It holds three subcards side by side, each a single row with the
name on the left and the value on the right: Passive Perception,
Passive Investigation, and Passive Insight. The subcards reuse the
vitals-item styling, including the same uniform `rounded-[7px]` radius on
all four corners. Tanadirian's parsed passives are 18 / 11 / 18. The card's
S/M/L toggle hides the section in small and medium modes; press L to bring
it back.

```mdx
<XmlCard character={tanadirian} display="large" />
```

<XmlCard character={tanadirian} display="large" />

## Notes

- **Powers:** parsed from the direct-child `<powers>` collection
  (`id-` keys, like every other Fantasy Grounds list); nested `<powers/>`
  inside inventory items is excluded. 108 of the 111 characters have at
  least one power.
- **Multiclass:** no character in the set currently has more than one
  class; the `Class A x / Class B y` rendering is untested against live
  data.
