# npm-only settings. pnpm 11 reads only registry/auth settings from .npmrc;
# pnpm-specific settings live in pnpm-workspace.yaml.

# Block lifecycle scripts from running during install
ignore-scripts=true

# Run an audit for known vulnerabilities as part of install
audit=true
