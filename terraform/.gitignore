# Terraform state files
*.tfstate
*.tfstate.*
*.tfplan

# Variable files with sensitive values
*.tfvars
*.tfvars.*

# Terraform directory
.terraform/

# .terraform.lock.hcl is intentionally tracked: it pins provider versions
# for every operator. Do not add it back here.
